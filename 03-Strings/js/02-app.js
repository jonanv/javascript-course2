const producto = 'Monitos 20 pulgadas';

console.log(producto);

// Conocer la longitud de una cadena
console.log(producto.length);

// Conocer la posición de una cadena
console.log(producto.indexOf('pulgadas'));

// Conocer si una cadena contiene otra cadena
console.log(producto.includes('pulgadas')); // true
console.log(producto.includes('Pulgadas')); // false